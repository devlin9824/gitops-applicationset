# Cấu hình tối giản nhất cho Bitnami Nginx
image:
  registry: docker.io
  repository: nginx
  tag: alpine

# Quan trọng: Bitnami mặc định chạy non-root port 8080
service:
  type: ClusterIP
  port: 8080
