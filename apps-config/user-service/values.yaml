# SỬA LẠI ĐOẠN NÀY
image:
  registry: docker.io
  repository: bitnami/nginx  # <--- Dùng ảnh của Bitnami, đừng dùng 'nginx' thường
  tag: "1.25"

# Giữ nguyên cấu hình port này để khớp với Chart
service:
  type: ClusterIP
  port: 8080

# Tắt bớt tính năng thừa
auth:
  enabled: false
