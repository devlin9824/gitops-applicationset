# SỬA LẠI: Dùng đúng Image của Bitnami để khớp với Chart
image:
  registry: docker.io
  repository: bitnami/nginx
  tag: "1.25"

# Cấu hình Service
service:
  type: ClusterIP
  port: 8080

# (Tùy chọn) Tắt bớt mấy cái lằng nhằng của Bitnami để chạy nhẹ hơn
auth:
  enabled: false
